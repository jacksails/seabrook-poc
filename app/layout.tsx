import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Seabrook Rumble - Brilliant by the Bagful',
  description: 'Upload your stomach rumble and discover your perfect Seabrook crisp flavour! Britain\'s original crinkle cut meets advanced audio analysis.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
} 